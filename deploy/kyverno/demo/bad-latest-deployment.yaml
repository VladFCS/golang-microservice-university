apiVersion: apps/v1
kind: Deployment
metadata:
  name: bad-latest-demo
  namespace: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bad-latest-demo
  template:
    metadata:
      labels:
        app: bad-latest-demo
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: app
          image: ghcr.io/vladfcs/golang-microservice-university-gateway-service:latest
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true

