apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: demo
  labels:
    app.kubernetes.io/name: inventory-service
    app.kubernetes.io/component: grpc
    app.kubernetes.io/part-of: microservices-grpc-kubernetes
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: inventory-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: inventory-service
        app.kubernetes.io/component: grpc
        app.kubernetes.io/part-of: microservices-grpc-kubernetes
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: inventory-service
          image: ghcr.io/vladfcs/golang-microservice-university-inventory-service@sha256:99ccfda21498a63b8727082068c43975b0792bca9332d8d137c7e27557296025
          imagePullPolicy: IfNotPresent
          env:
            - name: GRPC_PORT
              value: "50052"
            - name: HEALTH_PORT
              value: "8082"
          ports:
            - name: grpc
              containerPort: 50052
            - name: health
              containerPort: 8082
          readinessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 3
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
