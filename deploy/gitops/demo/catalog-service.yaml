apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: demo
  labels:
    app.kubernetes.io/name: catalog-service
    app.kubernetes.io/component: grpc
    app.kubernetes.io/part-of: microservices-grpc-kubernetes
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: catalog-service
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
