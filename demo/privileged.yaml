apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-privileged
  namespace: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-privileged
  template:
    metadata:
      labels:
        app: demo-privileged
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: app
          image: ghcr.io/vladfcs/golang-microservice-university-gateway-service:v1.0.0
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          securityContext:
            runAsNonRoot: true
            privileged: true

