apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-unsigned
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-unsigned
  template:
    metadata:
      labels:
        app: demo-unsigned
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: app
          image: ghcr.io/vladfcs/golang-microservice-university-gateway-service:unsigned-demo
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
